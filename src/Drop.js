/**
 * Класс реализует поведение инкапсулированного Drag
 * в момент когда перетаскивание закончилось и 
 * инкапсулированный объект надо передать объекту под указателем мыши
 */
 class Drag {
    constructor({

    })
}